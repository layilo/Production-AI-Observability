global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/ai-observability-rules.yml

scrape_configs:
  - job_name: ai-observability-api
    metrics_path: /metrics
    static_configs:
      - targets: ["observability-api:8000"]
